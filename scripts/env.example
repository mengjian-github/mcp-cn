# Supabase 配置（必须）
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# AI 配置（用于内容优化）
OPENROUTER_API_KEY=your-openrouter-api-key
AI_MODEL=openai/gpt-4o-mini

# GitHub 配置（可选，但推荐设置以避免API限制）
GITHUB_TOKEN=your-github-token

# 说明：
# 1. NEXT_PUBLIC_SUPABASE_URL: 你的Supabase项目URL
# 2. SUPABASE_SERVICE_ROLE_KEY: Supabase服务角色密钥（不是anon key）
# 3. OPENROUTER_API_KEY: OpenRouter API密钥，用于AI内容生成
# 4. AI_MODEL: 使用的AI模型，默认为 openai/gpt-4o-mini
# 5. GITHUB_TOKEN: GitHub个人访问令牌，用于提高API限额 